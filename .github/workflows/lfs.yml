name: Vector - LFS Object Poisoning
on: [pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true
      - name: Build
        run: |
          echo "=== LFS POISONING ==="
          echo "Git LFS objects are fetched separately from git history"
          echo "Attacker can replace LFS objects in their fork"
          echo "Workflow downloads and processes poisoned LFS content"
          echo "====================="
